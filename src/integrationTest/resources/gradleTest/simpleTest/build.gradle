buildscript {
    dependencies {
        classpath 'org.ysb33r.doxygen:doxygen:0.2'
    }
}

apply plugin: 'groovy'
apply plugin: 'org.ysb33r.doxygen'

version = '0.1'

dependencies {
    compile localGroovy()
    compile ':commons-cli:1.2'
}

task runGradleTest {
    dependsOn jar
}