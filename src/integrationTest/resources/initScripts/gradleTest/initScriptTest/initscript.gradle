
allprojects {
    buildscript {
        repositories {
            flatDir {
                dirs "${project.rootProject.projectDir}/../../repo"
            }
        }
    }

    task simpleTest << {
        println 'simpleTest is here'
    }
}

